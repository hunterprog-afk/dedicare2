# URL base degli endpoint pubblici del bot aziendale (report-ore-bot) usati
# dai moduli del sito (candidatura "Lavora con noi" e contatti — vedi
# src/lib/formsApi.ts). In produzione il default hardcoded punta a
# https://dedicare-report-ore-bot.fly.dev: questa variabile serve solo per
# puntare a un'istanza locale del bot durante lo sviluppo.
VITE_FORMS_API_BASE=http://localhost:3000
